# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Everything under any ./stage directory
*.tar.gz
*.jar

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Password/Credentials
.pwd
.pwdloc
creConnect/.pwdmongolocal
creConnect/.pwdmongoatlas

# Dependency directories (remove the comment below to include it)
# vendor/
.DS_Store
.idea/
.vscode/

# Folders where jar and *.tar.gz files will be staged, not to be included in git sync.
# also excluding these via *.tar.gz and *.jar definitions higher up.
stage/

devlab-hdfs-paimon-hdfs-cat/hdfs/base/stage
devlab-hdfs-paimon-hdfs-cat/stage/*
devlab-hdfs-paimon-hdfs-cat/data/*

devlab-hdfs-paimon-hms-cat/hdfs/base/stage*
devlab-hdfs-paimon-hms-cat/data/*
devlab-hdfs-paimon-hms-cat/stage/*

devlab-hms-postgres/data/*

devlab-hms-standalone/data/*
